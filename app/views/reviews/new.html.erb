<%# form_for @review do |f| %>
<%# f.collection_select :renter_id, @renters, :id, :name %>
<%# f.collection_select :landlord_id, @landlords, :id, :name %>
<%# f.number_field() %>
<%# f.submit %>
<% end %>